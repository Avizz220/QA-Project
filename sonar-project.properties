# Project identification
sonar.projectKey=taskmanager
sonar.projectName=Task Manager
sonar.projectVersion=1.0
sonar.host.url=http://localhost:9000

# Source code location
sonar.sources=backend/src/main/java,src
sonar.java.binaries=backend/target/classes
# Include both the Maven repository and project dependencies
sonar.java.libraries=C:/Users/Avishka/.m2/repository/**/*.jar,backend/target/*.jar

# Tests - commented out since we're skipping tests for now
#sonar.tests=backend/src/test/java
#sonar.java.test.binaries=backend/target/test-classes
#sonar.junit.reportPaths=backend/target/surefire-reports

# Encoding of the source code
sonar.sourceEncoding=UTF-8

# Exclude generated code or libraries if needed
# sonar.exclusions=**/generated/**

# Language
sonar.language=java

# Set custom working directory
sonar.working.directory=C:/temp/sonar-taskmanager